# Production Mode Configured
- Logo added to sidebar.
- Production build serving enabled in server.ts.
- SMS verification now uses real codes (stored in memory).
- Default port changed to 18520 as requested by user.
